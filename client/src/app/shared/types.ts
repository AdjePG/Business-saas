export enum ToastType {
  SUCCESS = "success",
  WARNING = "warning",
  ERROR = "error",
  INFO = "info"
}

export enum CardType {
  INFO = "info",
  ADD = "add"
}

export enum CardModalType {
  ADD = "add",
  UPDATE = "update",
  SHARE = "share",
  DELETE = "delete"
}